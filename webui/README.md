# Web UI

Backend'in `/` altından servis ettiği, build-tool'suz tek sayfalık arayüz. `docs/api_contract.md`'nin ince istemcisidir; iş mantığı içermez.

## Dosyalar

| Dosya | Görev |
|---|---|
| `index.html` | Sayfa iskeleti, 6 sekme + profil/harita/maç detayı/sağlık detay görünümleri, özgün harita SVG'si, tek satır config |
| `app.js` | API istemcisi + görünüm mantığı |
| `style.css` | Stil (tek koyu tema, mobil öncelikli) |
| `mock_api.js` | Backend hazır değilken kullanılan fetch stub'ı |

## Mock ile çalıştırma

`index.html`'deki config satırı varsayılan olarak mock'tadır:

```html
<script>window.APP_CONFIG = { USE_MOCK: true, API_BASE: "/api/v1" };</script>
```

Statik bir sunucuyla aç (fetch stub'ı `file://` ile de çalışır ama sunucu daha gerçekçidir):

```
cd webui
python -m http.server 8080
# http://localhost:8080
```

Mock, 14 kişilik gerçekçi bir roster (her oyuncuda 5 anahtarlı `role_ratings`), 14 maçlık geçmiş (8 karışık kadro + 6 sabit kadrolu "rekabet" maçı, bkz. nemesis notu) ve rol atamalı 3 dengeleme önerisi döner; API anahtarı olarak boş olmayan herhangi bir değer kabul eder (401 akışını denemek için anahtar istemini boş geçebilirsin).

Mock'ta bilerek bırakılmış kenar durumlar: bir maçta iki katılımcının `rating_change`'i `null` (delta "—"), başka bir maçta iki katılımcının `position`'ı `null` (rol "—", o maç rol evrenine girmez). `PUT /matches/{id}/positions` mock'u yerel maç state'ini günceller ve `{updated, role_matches_replayed}` döner — rol ratinglerini yeniden hesaplamaz (gerçek replay backend'de).

`GET /highlights/weekly` mock'u da pencereyi maç geçmişinden türetir (contract'taki kural: son 7 gün; o pencerede maç yoksa `end` = en son valid maç ve `fallback: true`). Mock maçlar 2026-08-02..09 tarihli olduğundan tarih ilerledikçe fallback yolu kendiliğinden devreye girer. Dosyanın başındaki iki bayrakla senaryo zorlanabilir: `HL_FORCE_FALLBACK = true` (pencereyi son maç haftasına kaydırır), `HL_EMPTY = true` (hiç valid maç yokmuş gibi davranır → UI boş durum).

`GET /nemesis` ve `POST /balance/nemesis` mock'ları da maç geçmişinden **türetilir** (aday birim = (çift, rol), eşik `encounters >= 3`, `weekly` aynı pencere kuralıyla). Ana geçmişte her çift bir rolde yalnız 1 kez karşılaştığı için mock'a sabit kadrolu 6 "rekabet maçı" eklendi (2026-07-30 … 08-09); ilk ikisi haftalık pencerenin dışında kalır, böylece `weekly` çift `all_time`'dan farklı çıkar ve UI'daki "Bu haftanın çifti" notu gerçekten test edilir. Beklenen: `all_time` = Teoman–Kaan (Orta, 3–3, %100), `weekly` = Baran–Emir (Üst, 2–2, %100), `active: "weekly"`. Dosyadaki iki bayrakla senaryo değiştirilir: `NEM_NONE = true` (çift yok → boş durum, düğme çizilmez), `NEM_WEEKLY_OFF = true` (`weekly` bastırılır → `active: "all_time"`, haftalık not çizilmez). `POST /balance/nemesis` mock'u 10 seçim / aktif çift / çiftin seçimde olması doğrulamalarını yapar (`422`, `409`) ve çifti karşı takımlara ayırıp ikisini de nemesis rolüne sabitleyerek 3 öneri döner.

Harita ekranı (GÖREV 4) ayrı bir uç kullanmaz, `GET /leaderboard` mock'undaki `role_ratings` yeterlidir. Mock'ta bir rol **bilerek boş** bırakıldı: dosyanın başındaki `EMPTY_ROLE = "UTILITY"` bayrağı o rolde kimsenin maçı yokmuş gibi davranır (beş anahtar yine döner, hepsi default prior) → haritadaki DESTEK baloncuğu soluk "—" görünür, pop-up "Bu koridorda henüz kimse oynamadı." yazar ve Enler ekranındaki Destek kartı da boş çıkar. `EMPTY_ROLE = null` yapılırsa beş rol de dolu olur. (Tutarlılık için `best_by_role` mock'u adayı "pencerede o rolde oynamış **ve** rol evreninde rating'i olan" diye süzer.)

`GET /players/{id}/stats` mock'u istatistikleri maç geçmişinden **türetir** (yalnız `status: "valid"` maçlar), böylece bir maçı void edince profil de tutarlı biçimde değişir. Maçsız oyuncu **Ece** (`matches_played: 0`) hiçbir mock maçına girmez ve contract'taki tüm boş yolları temsil eder: `winrate: null`, `kda: null`, `favorite_champion: null`, `favorite_role: null`, `synergy: []`. Bilinmeyen id → `404`.

## Backend'e bağlama

1. `index.html`'de `USE_MOCK: false` yap.
2. Backend'i çalıştır — `webui/` dosyalarını FastAPI StaticFiles ile `/` altından servis eder, ekstra bir şey gerekmez.
3. İlk açılışta sorulan API anahtarı, backend'in `.env`'indeki shared secret'tır; `localStorage`'a yazılır, 401 dönerse tekrar sorulur.

`API_BASE` göreli (`/api/v1`) olduğu için UI hangi origin'den servis edilirse backend'i orada arar; ayrı bir host'a bağlanmak gerekirse config'e tam URL yazılabilir (`"http://sunucu:8000/api/v1"`).

## Notlar

- Gösterilen birincil rating değeri `rating.score`'dur (harman engine); sıralama tablosunda skorun altında W/L çekirdeği (`ordinal`) ve `perf_avg` soluk ikincil satır olarak görünür, `perf_avg` null ise (harman-dışı version, score = ordinal) bu satır gizlenir.
- Rol ratingleri (`role_ratings`, GÖREV 0) iki yerde görünür: dengeleme ekranındaki oyuncu kartlarında kompakt şerit (`role-strip`; rol · puan · o roldeki maç sayısı), oyuncu profilinde **simgeli dolgu yayı** (`k2-gg`, GÖREV 24). `matches = 0` olan rol her ikisinde de soluk gösterilir (default prior, gerçek veri değil). Alan yoksa hiç çizilmez.
- **Oyuncu profili (GÖREV 1; yerleşim GÖREV 24'te K2-2 "Kaidenin İki Yanı" konseptinden BİREBİR taşındı):** sıralamada oyuncu adına dokununca `GET /players/{id}/stats` ile açılan, sekmesiz "detay" görünümü (`#view-profile`); üstteki "← Sıralamaya dön" ile kapanır, gezinmede Sıralama aktif kalır. İskelet ve ölçüler konsept dosyasından alınmıştır; sınıf adları `k2-` önekiyle ayrıştırılmıştır (konseptin `.hero/.body/.card/.duo/.lbl/.cap` gibi çok genel adları mevcut CSS ile çakışıyordu), rozet/madalyon parçaları mevcut `pb-` ad alanında kalmıştır. Sıra: **kaide** (`.k2-hero` — TAM GENİŞLİK, radial gradient zemin + pirinç zemin çizgisi; ad + puan ortalı, ortada 3 büyük madalyon vitrini, göstergeler kaidenin iki yanında aynaya bakan rakam blokları) → **rating tarihçesi grafiği + yanında rol yayları** (tek bölüm başlığı: "Rating ve roller") → **birlikte en iyi oynadıkların** → **rozet kuyruğu** → favori eşya. **Genişlik:** profil görünümünde `main`in 720px kısıtı kalkar (`showView` yalnız burada `.pa-full` basar) — kaide kenardan kenara, içerik 1240px'e kadar; diğer görünümler etkilenmez. Konseptte olup Teoman'ın kararıyla ALINMAYANLAR: puanın altındaki engine/formül satırı, vitrindeki "EN NADİR 1/2/3" etiketleri, vitrindeki nadirlik yüzdesi/çubuğu, konseptin kendi rol simgesi çizimleri (yerine ortak `posIconHtml`). Dar ekranda (≤900px) kaide iki sütuna düşer ve vitrin en üste geçer. Sinerji kartlarındaki isimler o oyuncunun profiline geçer. Contract'taki null durumları "—" ile karşılanır (`kda: null`, favoriler `null`, maçsız oyuncu); `synergy: []` → "Kayda değer sinerji yok". Puan ve rol ratingleri `GET /players` önbelleğinden gelir (profil isteği yalnız istatistikleri taşır).
- **Rozetler (GÖREV 11+12, katalog GÖREV 24'te 28 rozet; `pb-` öneki):** `GET /players/{id}/badges?include_locked=true` + nadirlik için bir kez `GET /badges`. Madalyon konseptin `.medal` bileşenidir: **sekizgen plaka** + merkeze giden faset çizgileri + iç halka + ortada rozetin simgesi (512'lik viewBox; gerçek PNG geldiğinde plakanın üstüne biner, dosya yoksa plaka görünür kalır). Kaidedeki **vitrin** EN YÜKSEK KADEMELİ 3 rozettir (`stellar > diamond > platinum > gold > silver > bronze`; eşitlikte `holders_pct` küçük → `count` büyük → katalog sırası) ve bu üçü aşağıdaki kuyrukta yeniden listelenmez. Çarpan (`×N`) madalyonun köşesindeki pirinç sayaç madenidir. **Kademe kümülatif SAYAÇLADIR ve asla düşmez** (eşikler backend'den `next_tier_count` ile gelir; UI "Platin kademesine 3 rozet kaldı" der, oran hedefi göstermez); `stellar` sayaçla değil GÖREVLE açılır (elmas eşiği + rozeti 3 maç üst üste kazanmak) ve ayrıcalıklı gökkuşağı/kırınım çerçevesi taşır — çerçevenin dönüşü `@property --pb-turn` ile animasyonludur, `prefers-reduced-motion`'da animasyon durur ama gradient (kırınım hissi) kalır. Rozet adı, açıklaması, kademe/hedef, Stellar görev ilerlemesi, **nadirlik** ("grupta 7 kişide") ve "en iyi an" bilgisi **bilgi baloncuğundadır** (`pb-tip`): hem vitrinde hem kuyrukta hover, dokunma ve klavye odağıyla açılır, Esc / dışına dokunma kapatır; aynı metin ekran okuyucuya `pb-sr` ile düğmenin adı olarak da verilir.
- **Haftanın enleri (GÖREV 2):** "Enler" sekmesi (`#view-highlights`), tek istek `GET /highlights/weekly`. Üstte pencere satırı ("5–12 Ağu arası"; ay sınırını aşarsa "30 Tem – 6 Ağu arası"), `window.fallback: true` ise yanında pirinç renkli "(son maç haftası)" notu. Altında sırayla: **Haftanın Oyuncusu** (vurgulu büyük kart — ad, güncel puan, "pencerede N maç"), **Yıldız Rukisi** (ad, 2 ondalıklı delta — pozitif yeşil / negatif kırmızı, "pencerede N maç") ve **Rol enleri** (Üst/Orman/Orta/Alt/Destek kartları; ad, rol puanı, o roldeki maç sayısı). Dolu kartlar `<button>`'dır ve oyuncu profiline gider; `null` alanlar dokunulamaz, soluk "—" kartı olarak kalır (`.hl-none`; global `.empty` sınıfı kart içinde kullanılmaz, o ortalı boş-durum paragrafı içindir). Üç alan da null ise (hiç valid maç yok) tek satır boş durum yazılır.
- **Nemesis (GÖREV 3):** Enler ekranının en altındaki bölüm, tek istek `GET /nemesis` (Enler yüklenirken `/highlights/weekly` ile paralel çekilir; **bu istek düşerse bölüm hiç çizilmez**, ekranın kalanı çalışmaya devam eder — `/nemesis` bilmeyen bir backend'e karşı güvenli). Gösterilen çift **tüm zamanların** çiftidir: iki isim karşılıklı (ikisi de profile gider), ortada koridor + galibiyet skoru (`wins[0]–wins[1]`) + karşılaşma sayısı, altında `closeness` yüzdesi ("%100 başa baş"). `weekly` doluysa ve all_time'dan farklıysa (rol + çift karşılaştırması) tek satır not düşülür: "Bu haftanın çifti: … ". Maç kurmada kullanılacak çift (`active`) her iki yerde de "maç bu çiftle kurulur" rozetiyle işaretlenir. `all_time` null ise bölüm "Nemesis için en az 3 koridor karşılaşması gerekiyor." yazar. "Nemesis maçı kur" düğmesi yalnız `active != null` iken çizilir.
- **Nemesis modu (Dengele):** "Nemesis maçı kur" Dengele sekmesine geçip modu açar. Rozet (`#nemesis-mode`) çifti ve koridoru yazar, çiftin ikisi de seçili değilse eksik isimleri sayar; sağdaki × modu kapatır (önceki öneriler de temizlenir). Modda çiftin roster kartları kesik çizgili ve pirinç adla işaretlenir. "Dengele" düğmesi `POST /balance/nemesis`'e gider; yanıt `/balance` ile aynı şekilde çizilir, ek olarak öneri başlığının altına "Nemesis maçı: A vs B — Rol (bu haftanın/tüm zamanların çifti)" notu düşer ve çiftin satırları vurgulanır. `422` (çift seçimin dışında) ve diğer hatalarda `detail` her zamanki gibi toast'ta gösterilir; `409` (aktif çift kalmamış) durumunda ek olarak mod otomatik kapatılır ve mesaja "Nemesis modu kapatıldı." eklenir. Seçim akışı ve 10 kişi kuralı değişmez.
- **Rol enleri haritası (GÖREV 4):** koridor haritası üzerinde beş rol baloncuğu (`#view-map`), tek istek `GET /leaderboard` (yeni endpoint yok — veriler yanıttaki `role_ratings`). Harita **özgün, stilize, satır içi SVG**'dir: Riot varlığı/kopyası değildir, dış görsel/CDN kullanılmaz (`index.html` içinde ~3 KB path; köşegen nehir, üç koridor, iki üs, dört orman bölgesi, kule noktaları). `viewBox="0 0 100 100"` ile ölçeklenir, kutu `aspect-ratio: 1/1` + `max-width: 460px`. Baloncuklar SVG'nin değil, üstteki HTML katmanının parçasıdır (yüzde konumlu `<button>`) — böylece metin harita küçülünce küçülmez. Konumlar: ÜST üst koridorun ortası, ORMAN üst-sol orman bölgesi, ORTA merkez, ALT alt koridorun ortası, DESTEK alt koridorun yanı. Her baloncukta rol kısaltması + o rolün **birincisi** + rol puanı (1 ondalık). Rolde hiç oynayan yoksa soluk kesik çizgili "—" (`.rb-none`; global `.empty` sınıfı **kullanılmaz** — o paragraf sınıfının `padding: 40px` değeri kutuyu bozuyor).
- **Rol sıralaması pop-up'ı:** baloncuğa dokununca `#role-modal` açılır (API anahtarı modalıyla aynı `.modal-backdrop` deseni, `.modal-list` varyantı). İçerik: o rolde **≥1 maçı olanların** tam sıralaması — rol score azalan → o roldeki maç sayısı azalan → ad alfabetik (`tr`); satırlar sıra no · ad · rol puanı · "N maç". `matches = 0` (default prior) sıralamaya girmez. Ada dokununca pop-up kapanır ve oyuncu profiline gidilir. Kapatma: × düğmesi, dışına tıklama (yalnız backdrop'un kendisi) ve Esc; kapanınca odak baloncuğa döner. Boş rolde tek satır: "Bu koridorda henüz kimse oynamadı." Backend `role_ratings` döndürmezse beş baloncuk da "—" olur, ekran çalışmaya devam eder.
- **Haritaya giriş (ölçüm kararı):** harita alt çubukta 6. sekme **değildir**, profil gibi "detay" görünümüdür. Ölçüm (canvas `measureText`, sekme yazı tipi `700 11px Bahnschrift`, ≤400px ölçeği): 320px'de 6 sekmenin her birine düşen metin payı `320/6 − 4 = 49,3px`, en geniş etiket "SIRALAMA" ise **53,7px** — taşıyor. 10px'e inince Bahnschrift'te 48,8px (0,5px pay) olsa da Bahnschrift bulunmayan cihazlarda yedeğe düşüyor ve orada da taşıyor (Arial Narrow 53,6px, system-ui 52,1px). O gün 5 sekmede pay 60px olduğu için çubuk korunmuştu. (GÖREV 17 notu: alt çubuk sol gezinme paneline dönüştü, sekme genişliği derdi kalmadı; harita yine de sekme değildir, o karar ölçümden değil kapsam tercihinden gelir.) Giriş iki yerden: Enler ekranındaki "Rol enleri" başlığının yanında **"Haritada gör →"** ve Sıralama tablosunun üstünde **"Rol enleri haritası →"**. Geri düğmesi geldiği yere göre yazar, gezinmede o sekme aktif kalır (`state.mapFrom`); haritadan açılan profilin geri düğmesi "← Haritaya dön"dur ve sekme yine kökene çözülür (`tabOf` özyinelemeli).
- **META: şampiyon kademeleri (GÖREV 16):** patch'in güçlü şampiyonlarını S/A/B olarak gösteren görünüm (`#view-meta`, `mt-` öneki). Veri **backend'den gelmez**: statik `assets/meta/tiers.json` (api_contract §8; `deploy/fetch_meta.py` üretir, Teoman onaylayıp commit'ler) `window.fetch` ile bir kez çekilip önbelleğe alınır — istek `X-API-Key` taşımaz, `USE_MOCK` yolundan geçmez, dd- varlık katmanıyla aynı desendedir. Üstte sağa yaslı "Patch 16.16 · 15 Ağu" başlığı (`patch`/`updated` bozuksa o parça yazılmaz), altında süzgeç çubuğu: **TÜMÜ** (yıldız + metin, varsayılan) + beş rol (ortak `posIconHtml`, ikon yoksa kısaltma metnine düşer). Süzgeçler gerçek `<button>`'dır, seçili olan `aria-pressed="true"` taşır; süzgeç değişiminde **çubuk yeniden kurulmaz** (yalnız sınıf/aria güncellenir) — `innerHTML` ile kurulsaydı basılan düğme silinir ve klavye odağı body'ye düşerdi. Altında S/A/B üç dikey sütun; sütun tonu paletten türetilir (S `--brass`, A `--blue`, B `--muted`; başlık harfi, üst kenar ve portre halkası aynı `--mt-tone`). Her şampiyon yuvarlak portre (dd- ikonları, yoksa baş harf yer tutucusu) + ad. **Sütunun içi de ızgaradır (GÖREV 17):** şampiyonlar tek sıra inmek yerine `repeat(auto-fill, minmax(58px, 1fr))` ile satır başına sığan kadar dizilir. Sayı sabitlenmez, sütun genişliğinden türer; `main` 720px'de kapandığı için bir kademe sütununun içi en fazla ~202px olur ve formül orada tam **üçe** oturur (masaüstünde 3'lü satır, hücre 62,1px), 480–719px'te 2, altında 1. Ölçüm (gerçek DOM, iframe): 320/360/400px → 1 sıra; 480/560/640 → 2; 720/900 → 3; her genişlikte `scrollWidth − clientWidth = 0` (ne `.mt-cols`, ne `.mt-col`, ne `.mt-champ`, ne belgede yatay taşma). Ad ve rol rozeti ≥560px'te bilerek büyütülmez (eski 13px/15px kaldırıldı): 62px'lik hücrede 11px'te dosyadaki 98 adın hiçbiri sarmıyor, 13px'te "Mordekaiser"/"Fiddlesticks" kelime ortasından bölünüyordu. **TÜMÜ görünümünde** şampiyon EN İYİ kademesinde bir kez listelenir ve adının altında o kademeyi tuttuğu koridorların rol simgeleri durur (daha düşük kademedeki rolü sütunun anlamıyla çelişeceği için orada gösterilmez — o bilgi rol süzgecinde); rol süzgecinde yalnız o koridorun listesi, rozetsiz. Sıra dosyadan gelir (kademe → kanonik rol sırası → dosya sırası), UI yeniden sıralamaz. `champions.json`'da olmayan ad da **listelenir**, yalnız portre yer tutucuya düşer; şema dışı hücreler (sayı, boş dize, eksik anahtar) sessizce elenir. Dosya yoksa/bozuksa yalnız bu görünüm hata kutusu gösterir (sağlık ekranı deseni: başlık · ayrıntı · ne yapmalı; 404 → "…alınamadı (HTTP 404)", bozuk JSON → "…beklenen şemada değil") — yükleyici **throw etmez**, uygulamanın kalanı etkilenmez.
- **META artık kendi sekmesi (GÖREV 17, Teoman kararı):** GÖREV 16'da meta, 320px ölçümü yüzünden Enler'in içinden açılan "detay" görünümüydü. Karar revize edildi: **META ana gezinmenin 6. sekmesidir** (`common.tab_meta`, TR/EN "META"), üzerine başka modüller bineceği için kendi girişini hak ediyor. Enler'deki `.mt-entry` / "Meta →" bağlantısı, `#btn-meta-back` geri düğmesi, `state.metaFrom` ve `DETAIL_FROM.meta` **kaldırıldı** — meta artık normal sekme gibi çözülür (geri düğmesi yok), `highlights.meta_link` iki sözlükten de silindi.
- **6 sekmenin 320px çözümü (TARİHSEL NOT — GÖREV 17 ile alt çubuk sol panele dönüştü, bu clamp CSS'i kaldırıldı):** eski çubuk basamaklı `font-size: 12px → ≤400px'te 11px` kullanıyordu ve 6. sekmede kırpıyordu. Yeni kural puntoyu sekme genişliğine bağlar: sekme her zaman `100vw/6 ≈ 16,67vw` olduğu için `font-size: clamp(9.5px, 2.8vw, 12px)`; yatay dolgu ≤460px'te 2px→1px, ≤360px'te 0'a, harf aralığı 0,04em → 0,01em → 0'a iner. Alt sınır (9,5px) dar Bahnschrift'e göre değil, **yedek yazı tipine** göre seçildi: Bahnschrift yalnız Windows'ta var, telefonda system-ui'ye düşülür ve ölçüler ~%10 genişler. Ölçüm (Chrome, gerçek DOM, `style.css` yüklü iframe, `Range.getBoundingClientRect()` ile metin genişliği; "slack" = kutu payı − metin, negatifi kırpma demektir):

  | genişlik | punto | kutu payı | TR "SIRALAMA" slack | EN "BALANCE" slack | TR yedek font | EN yedek font |
  |---|---|---|---|---|---|---|
  | 320px | 9,50px | 53,33px | +7,4px | +11,1px | +4,3px (system-ui) / +2,9px (Arial) | +10,0 / +6,6px |
  | 360px | 10,08px | 60,00px | +11,6px | +15,5px | +8,3px / +6,8px | +14,4 / +10,7px |
  | 400px | 11,20px | 66,66px | +10,3px | +14,8px | +6,6px / +5,0px | +13,5 / +9,5px |

  Altı sekmenin **hiçbirinde** kırpma yok: her ölçümde sekme başına `scrollWidth − clientWidth = 0` ve çubukta `scrollWidth − clientWidth = 0`. Arial sütunu bilinçli olarak kötümser: stack'te Arial yok (`Bahnschrift, "Arial Narrow", system-ui, sans-serif`), Roboto/Arial genişliğinde bir gövde fontuna düşülse bile pay pozitif kalıyor.
- Profil görünümü dört yerden açılır (sıralama, enler, harita, maç detayı); geldiği görünümün sekmesi aktif kalır ve geri düğmesi ona göre "← Sıralamaya dön" / "← Enlere dön" / "← Haritaya dön" / "← Maç detayına dön" yazar (`state.profileFrom`).
- **Maç detayında ada tık (GÖREV 15):** satır başlığındaki oyuncu adı beş sekmede de (Build/Gold/Hasar/CS/Vizyon) profile giden gerçek `<button>`'dır (`.md-name-btn`; Tab ile odaklanır, Enter/Space çalışır, şampiyon alt yazısı tıklama alanına dahildir). Görünüm değişmez: düğme `justify-self: start/end` + `max-width: 100%` ile metin genişliğinde durur, kırpma iç span'dadır (`text-overflow` doğrudan `button` üzerinde her tarayıcıda çalışmaz), tıklanabilirlik yalnız hover/odakta pirinç renk + noktalı alt çizgi ile belli olur (alt çizgi `text-decoration`, `border` değil → satır yüksekliği değişmez). TOPLAM satırının takım etiketleri tıklanmaz; rolü bilinmeyen ("?") satırlar tıklanır. Ölçüm (317px genişlik, aynı satırın span/buton A/B'si): satır yüksekliği 17,50 px ↔ 17,50 px, rol göstergesinin merkezden sapması 0,00 px ↔ 0,00 px, yatay taşma yok.
- **Geri zinciri (profil ⇄ maç detayı):** iki görünüm birbirini açabildiği için tek "nereden geldim" alanı döngüye giriyordu; artık detaydan detaya geçişte terk edilen görünümün tam bağlamı (`state.backStack`: hangi oyuncu / hangi maç + o görünümün kendi geri hedefi + grafik aralığı / seçili stat) bir yığına konur, geri düğmesi kareyi yığından çeker. Böylece profil A → maç → profil B → geri → **aynı maç** → geri → **profil A** → geri → Sıralama zinciri derinlikten bağımsız tutarlıdır. Sekmeye basmak zinciri terk eder (yığın boşalır); yığın kopmuşsa geri düğmesi güvenli sekmeye düşer, asla döngüye girmez. `tabOf` de bu yüzden döngü korumalıdır: ad ikinci kez gelirse zincirin kökü (yığının dibi) kullanılır.
- **Gezinme sol paneldedir (GÖREV 17, K1 "Sade Ray"; `sb-` öneki):** 6 sekme (Dengele · Sıralama · Enler · Geçmiş · META · Manuel) değişmedi, alt çubuğun görsel dili dikeye çevrildi. Masaüstünde (≥880px) panel **hep açık** sabit sütundur (200px, `--sb-w`); aktif öğede 3px pirinç sol şerit (eski `border-top`'un dikey karşılığı). Mobilde (<880px) üst bardaki hamburger (`#sb-burger`) soldan kayan çekmeceyi açar: scrim tıkı ve Esc kapatır, Tab odak çekmecedeki düğmeler arasında döner (basit tuzak), açıkken `body.sb-lock` scroll'u kilitler; kapatan etkileşim odağı hamburger'a iade eder. Dil (TR/EN) ve API anahtarı düğmeleri panelin **alt bloğundadır** (`.sb-bottom`; mobilde çekmecenin altı). Liste düzdür ama gruplamaya hazırdır: ileride `.sb-list` içine `.sb-gap` ayracı + başlık eklenebilir. Harita, profil, maç detayı ve collector sağlığı sekme **değildir**: ilgili ekrandan açılan detay görünümleridir.
- Sıralama satırındaki eski rol açılırı (chevron) kaldırıldı: aynı bilgiyi profil daha okunur biçimde (simgeli yaylarla) gösteriyor, tek satırda iki ayrı dokunma hedefi tutmanın karşılığı yoktu. Ada dokunma artık tek anlam taşır: profili aç.
- Dengeleme yanıtı her zaman rol atamalıdır: `team_100`/`team_200` = `[{player_id, position}]`. Takımlar TOP → JUNGLE → MIDDLE → BOTTOM → UTILITY sırasıyla, Türkçe etiketlerle (Üst/Orman/Orta/Alt/Destek) listelenir.
- Rol düzeltme: maç kartındaki "Rolleri düzenle" 10 katılımcı için rol seçici açar; "Rolleri Kaydet" yalnız **değişen** rolleri `PUT /api/v1/matches/{id}/positions` ile gönderir (`{"positions": {"<player_id>": "TOP"|null}}`) ve yanıttaki `updated` / `role_matches_replayed` bilgisini toast'ta gösterir. Ana rating etkilenmez; kaydedince maç listesi ve roster önbelleği tazelenir. Manuel girilen maçlarda roller boş geldiğinden bu panel o maçları rol evrenine sokmanın yoludur.
- **Kaydedilmemiş rol seçimleri kaybolmaz (fix, 2026-08-19):** seçimler DOM'da değil `cp.roleDraft`'ta (maç id → `{player_id: rol}`) tutulur; panelin her yeniden çizimi (arama, durum süzgeci, sekme değişimi, idari eylem sonrası tazeleme, dil değişimi) düzenleyiciyi taslaktan geri yükler — `data-original` yine sunucudaki değerdir, yani kısmi güncelleme ölçütü bozulmaz. Sunucudaki değere geri dönen seçim taslaktan düşer; farklı olan seçim `.re-dirty` ile işaretlenir. Seçimleri atacak her yol (düzenleyiciyi kapatma, başka maçın düzenleyicisini açma, sekme değişimi, paneli kilitleme) **önce onay sorar** (ad düzenlemesindeki desen; `control.unsaved_roles_confirm` / `control.unsaved_both_confirm`), iptal edilirse hiçbir şey değişmez. Önceki davranışta seçimler sessizce sıfırlanıyordu ve kullanıcı bunu "roller kaydedilmiyor" olarak görüyordu.
- "Dengele" butonu tam 10 seçim olmadan aktifleşmez (asıl doğrulama backend'de, `422`).
- **Kontrol Paneli (fix-2, `cp-` öneki):** Geçmiş kartlarındaki herkese açık void düğmesi kaldırıldı; void/unvoid/replay ve oyuncu adı düzeltme sol paneldeki şifre korumalı "Kontrol Paneli" sayfasındadır. Şifre yalnız bellekte tutulur (`state.adminKey`) — localStorage/sessionStorage'a **yazılmaz**, her sayfa yenilemesinde yeniden sorulur; doğrulama yan etkisiz `GET /admin/ping` (204) iledir ve idari çağrılar `X-API-Key`'e **ek olarak** `X-Admin-Key` taşır. `503` (sunucuda `ADMIN_KEY` yok) ile `403` (şifre yanlış) ayrı mesaj gösterir; her ikisinde de panel kilitlenip giriş ekranına döner. Mock modunda geçerli şifre `mock-admin-key`'dir (sahte sabit; `window.MOCK_ADMIN_KEY_MISSING = true` ile 503 yolu denenebilir).
- Void artık geri alınabilir: panelde void maçın satırında "Geri al" (`POST /matches/{id}/unvoid`) durur. Rulet maçında ikisi de gösterilmez (backend 409 verir), yerine kısa bir not basılır.
- Hata yanıtlarındaki `detail` alanı kullanıcıya aynen gösterilir (backend Türkçe döner).
- `GET /matches` yanıtının alan bazlı şekli contract'ta örneklenmediği için varsayılan şekil `docs/CHANGE_REQUESTS.md`'ye yazıldı.
