# Change Requests

- [Teoman → orkestratör] [2026-08-17] **GÖREV 21-FIX (modules/module-21-FIX): Seçim Danışmanı kullanıcı geri bildirimi + yeni "Eşleşme Optimizasyonu" sayfası.** Kullanıcı deneyimi bulguları: çok adımlı/zor kullanım (10 satır girmek ağır), "erken/geç oyuna ölçeklenir (sezgisel)" rozeti anlamsız bulundu, grup W/R rozeti alakasız bulundu, eksiklik sınıfları beğenildi ama zenginleştirilmeli ("karşısı kırılgan → suikastçı al" / "karşısı dayanıklı → can eriten taşıyıcı al" gibi). **Kararlar (Teoman):** (1) S3 ekranı RAFA — silinmez, koduna DOKUNULMAZ, sekmesi yerinde kalır (ileride dönülecek; zengin eksiklik sınıfları o dönüşte ele alınacak). (2) Yeni ayrı sayfa "EŞLEŞME OPTİMİZASYONU", tasarım = 5 mockup'tan **M1 "Sahne"**: ortada büyük rol simgeleriyle "ROL SEÇİMİ" → seçimde sayfa aşağı kayar → "Karşındaki oyuncu ne seçti?" (varsayılan "Henüz seçmedi") → rakipsizken o rolün META önerileri (tier+WR rozetli), rakip seçilince o koridorda counter sıralaması (counters.json — mevcut veri, yeni veri işi YOK). Sezgisel early/late ve grup W/R rozetleri bu sayfada YOKTUR. **Orkestratör tanımları:** yeni sol panel sekmesi; advisor.js'in veri yükleyicileri yeniden kullanılır (kod kopyalanmaz); yeni sınıf öneki `mo-`; i18n tr+en; backend/DB/deploy değişikliği YOK.

- [Teoman → orkestratör] [2026-08-17] **GÖREV 21 (modules/module-21): seçim danışmanı ekranı.** Teoman kararları (AskUserQuestion, araştırma raporu sunularak): **(1) Veri = vendored JSON, META deseni** — OP.GG'nin ZATEN çekilen tek isteğinden tier + win_rate + pick_rate + counter kayıtları; canlı dış API reddedildi. **(2) LoLalytics tam matrisi REDDEDİLDİ** — ToS'unda açık "üçüncü taraf kullanamaz" ifadesi + public repo'da veri yeniden dağıtımı + ~300 istekli kırılgan scrape riskleri anlatıldı, Teoman bilerek OP.GG karma havuzunu seçti: ~10 counter kartı = 1-3 gerçek veri + ters yön taraması + sınıf sezgiseli + tier dolgusu (rozetlerde veri/sezgisel ayrımı görünür). **(3) Grup verisi yalnız BİLGİ ROZETİ** ("Ömer: 3W-1L") — öneri sıralamasını etkilemez; `GET /matches`'tan istemcide türetilir, YENİ ENDPOINT YOK. **(4) v1 sinyalleri:** meta tier+winrate, kompozisyon açıkları (AD/AP + öncü/taşıyıcı — DD tags/info), counter (karma havuz), early/late (v1'de DD sınıf SEZGİSELİ, rozeti "sezgisel" etiketli; sayısal kaynak v2 araştırması — hiçbir kaynakta ucuz/güvenilir bulunamadı). **(5) Sunum:** gerekçe rozetli 5-8 kart; konsept turu (S1/S2/S3) sonucu seçim **S3 "Analiz Paneli"** (sol yarı giriş listeleri, sağ yarı AD/AP denge çubukları + eksik/tehdit rozetleri + kart ızgarası; geniş görünüm istisnası gerekir). **Orkestratör tanımları (teknik):** analiz tamamen istemci tarafında; `tiers.json` şema genişlemesi + YENİ `counters.json` + `champions.json`'a tags/info (spec: api_contract §8); ekran yeni sol panel sekmesi; iki liste (biz/karşı), rol dropdown'u karşı tarafta "Bilinmiyor" seçenekli; backend/rating/collector/DB değişikliği YOK.

- [Teoman → orkestratör] [2026-08-17] **GÖREV 20 (modules/module-20): Manuel ekleme ekranı kaldırılır.** Kapsam yalnız web UI — backend'in manuel ingest/positions endpoint'leri ve contract DEĞİŞMEZ (ekran siliniyor, API değil). **Orkestratör tanımları:** Manuel görünümü + sol panel satırı + yalnız ona ait i18n anahtarları (iki sözlükten birden) ve CSS silinir; collector sağlık paneli (GÖREV 13, girişi Manuel'deydi) kendi sol panel satırına taşınır ("Sağlık"/"Health") — GÖREV 17 kabulü gereği panele satır eklemek serbest.

- [Teoman → orkestratör] [2026-08-16] **GÖREV 18 (modules/module-18): Geçmiş kartındaki rating deltası yanıltıcı.** Kullanıcı raporu: blend20 sonrası kaybedenler + alabildiği halde Geçmiş hâlâ "kazanan ~+1, kaybeden ~−1" gösteriyor. Teşhis (orkestratör): veri gerçek ama YANLIŞ METRİK — kart `rating_change.mu_after - mu_before` (saf W/L çekirdeği) basıyor; çekirdek delta tasarım gereği hep sonuç yönündedir ve blend20'de skorun yalnız %20'sini temsil eder. **Karar: [Teoman, 2026-08-16, AskUserQuestion] Kart EFEKTİF SCORE FARKINI gösterecek.** Uygulama: `GET /matches*` `rating_change` nesnesine `score_before`/`score_after` eklenir (tanım rating-history `score_after` ile birebir; kümülatif P_avg, rating paketi yardımcılarıyla, 2 ondalık, replay determinizmi şart); web UI Geçmiş delta'sı `score_after - score_before` olur; mu/sigma alanları nesnede aynen kalır (kaldırılmaz). Spec: api_contract §3. DB/rating/collector değişikliği YOK.

- [Teoman → orkestratör] [2026-08-16] **GÖREV 17 (new_modules.md, yeniden tanımlanmış hali): frontend rebuild — alt sekme çubuğu → sol navigasyon.** (Eski "GÖREV 17 sinerji formülü" maddesi new_modules.md'de GÖREV X oldu; o ayrı ve hâlâ tartışma aşamasında.) Teoman kararları (AskUserQuestion): (1) süreç = önce 4 konsept artifact (K1 Sade Ray / K2 İkonlu Panel / K3 HUD / K4 Bütünleşik), seçim **K1**; (2) liste DÜZ, yapı gruplamaya hazır; (3) masaüstünde panel HEP AÇIK sabit (daraltma yok); (4) dil (TR/EN) + API anahtarı kontrolleri panelin alt bloğuna taşındı. **Orkestratör tanımları (teknik):** `sb-` önekli kabuk (`#sb-app`/`#sb-nav`/`#sb-scrim`/`#sb-burger`), <880px çekmece + scrim + `body.sb-lock` + basit odak tuzağı; sekme seti/etiketleri ve `data-view`/`TAB_OF` mekanizması birebir korundu; 2 yeni i18n anahtarı (`common.nav_aria`, `common.menu_btn`); tabbar kalıntıları (body alt payı, action-bar hesabı) süpürüldü. PR #49. Bilinen, GÖREV 17 ÖNCESİNDE DE VAR OLAN dar-ekran taşmaları takip işi olarak açık: Sıralama ≤~330px (~28px, EN rating-sub nowrap), Geçmiş takım sütunları ~<410px (200px×2 sabit genişlik).
  **Ek [orkestratör, 2026-08-16, teknik düzeltme]:** GÖREV 17 deploy'unda tarayıcılar eski cache'li style.css'i yeni index.html ile birleştirdi (statik yanıtlarda Cache-Control yoktu) → bir defalık stilsiz görünüm. Düzeltme: webui statik yanıtlarına `Cache-Control: no-cache` (ETag/304 revalidasyonu; `no-store` değil) — PR #52, backend 251→260 test. API yanıtları etkilenmez.

- [Teoman → orkestratör] [2026-08-16] **Harman ağırlığı revizyonu: W/L %20 + performans %80 — `openskill-pl-blend20-v1`.** Teoman'ın gerekçesi: "sıralamada çok kötü oyuncular sadece W/L sayesinde çok yukarıdalar." Karar öncesi orkestratör, canlı verinin (19 valid maç, 18 oyuncu, salt-okur GET) tam replay simülasyonunu w=0.50/0.25/0.20 için koşturdu; w=0.50 kolonu canlı leaderboard'la 18/18 birebir eşleşerek yöntemi doğruladı. Bulgular: %25 ve %20 neredeyse özdeş sıralama üretir (tek fark 3.-4. sıra takası); tarif edilen vakalar düşer (STAJYERARKACİZGİ 6.→12., kimsesiz34 10.→13., XqRahSe4d 14.→17.), yüksek-perf düşük-W/L oyuncular yükselir (SoSiSwithSaLaM 7.→5., utK 9.→6.). Riskler Teoman'a sunuldu ve kabul edildi: w=0.20'de sıralama fiilen P_avg−3σ karakterine yaklaşır (mu yalnız tepeyi ayırt eder); küçük örneklemli oyuncular (3-5 maç) perf'leriyle hızlı yükselip/düşebilir. **Karar: [Teoman, 2026-08-16, AskUserQuestion] %20 W/L — %80 perf.** Uygulama: yeni version `openskill-pl-blend20-v1` (MU_0=25, K=20, W=0.8; W/L çekirdeği ve perf_score fonksiyonları blend50 ile birebir aynı — yalnız harman ağırlığı değişir); rol evreni aynı version'la işler; blend50 tanımlı kalır. Geçiş iki evrende REPLAY gerektirir (deploy sonrası canlıda `POST /admin/replay`). Spec: rating_contract v3 "Harman Engine — blend20"; api_contract aktif version referansları güncellendi; CLAUDE.md karar #1 üçüncü kez revize edildi.

- [Teoman → orkestratör] [2026-08-15] **GÖREV 16 (new_modules.md): Collector Remaster.** Teoman kararları (AskUserQuestion): (1) arayüz **tkinter** (sıfır bağımlılık; durum bandı + gömülü log + butonlar: canlı başlat/durdur, backfill, eşya backfill dry-run→uygula akışıyla, ayarlar); (2) güncelleme v1 = **bildir + indirme linki** (açılışta GitHub Releases /releases/latest kontrolü; erişilemezse sessiz geç; oto-güncelleme v2'ye ertelendi); (3) yayınlama = **CI release** (`v*` etiketi → windows-latest → build.bat → exe GitHub Release'e; `.github/workflows/release.yml`; etiket-sürüm tutarlılık kontrolü var). Fazlama: A release hattı → B sürüm kontrolü → C tkinter arayüz (sihirbaz tkinter diyaloglarına taşınır, exe --windowed'a geçer; CLI komutları terminalden çalışmaya devam eder). SmartScreen uyarısı kabul (GÖREV 5 kararı sürüyor).

- [Teoman → orkestratör] [2026-08-15] **Favori karakter kuralı değişti:** "en çok oynanan" → "EN FAZLA MAÇ KAZANILAN" (yüzde değil, mutlak galibiyet sayısı — Teoman'ın açık talebi). Kırılım: galibiyet → maç sayısı → ad; yanıta `wins` eklendi (api_contract §2). Web UI kartı galibiyet sayısını gösterecek şekilde güncellenir; başka davranış değişmez.

- [Teoman → orkestratör] [2026-08-15] **META sayfası: tier listesi sekmesi.** Teoman kararları: (1) tasarım = konsept **M1** (rol filtreleri resmî ikonlarla üstte, TÜMÜ varsayılan; S/A/B dikey sütunlar; şampiyon portre+ad; TÜMÜ'de en-iyi-tier + rol rozetleri — 3 artifact'tan seçildi, örnek: image.png); (2) veri kaynağı **YARI OTOMATİK** (seçenek 2): betik topluluk kaynağından çeker, fark gösterilir, Teoman onaylayıp commit'ler — otomatik cron yok. **Orkestratör tanımları:** veri `webui/assets/meta/tiers.json` (şema api_contract §8); adlar DD görünen adlarıyla doğrulanır; `deploy/fetch_meta.py` onaysız YAZMAZ; kaynak seçimi araştırma worker'ının canlı probuyla belirlenir (JSON veren, rol bazlı, tek istek — scraping ToS gri alanı Teoman'a bildirildi ve kabul edildi). Alt çubukta 6. sekme ≥320px ölçüm kuralına tabidir (GÖREV 4 kararı); sığmazsa Teoman'a fallback sorulur. Backend/rating/collector değişikliği YOK.

- [Teoman → orkestratör] [2026-08-14] **GÖREV 14 (new_modules.md): eşya build'leri — BUILD sekmesi + görsel altyapısı.** Teoman kararları (AskUserQuestion): (1) görseller BUILD-TIME VENDORING ile (deploy imajı kurulurken Data Dragon'dan sabit patch indirilir; site kendi kendine yeterli kalır, repo'ya görsel girmez); (2) favori eşya = kariyerde en sık çıkılan, TRINKET/TÜKETİLEBİLİR HARİÇ, profil favoriler şeridinde ikonlu kart; (3) eski maçlar `backfill-items` ile doldurulur (backfill-positions deseni, raw_archive'dan). Ayrıca görev metninden: BUILD sekmesi stat sekmelerinin EN BAŞINA; şampiyon + eşyalar RESİMLE; hover'da ad/detay; tıklama yok. **Orkestratör tanımları (teknik):** ingest'e opsiyonel `participants[].items` (0-7 int, ham sıra; MH `item0..6`'dan 0'lar atılır); migration 0005 `match_participants.items_json`; `GET /matches*` katılımcılarına nullable `items` (NULL=bilinmiyor, []=boş); `PUT /matches/{id}/items` (üzerine yazar, replay YOK); player stats'a `top_items` (ham sayım; favori SEÇİMİ tags'le web UI'da — backend eşya meta verisi bilmez); varlık yerleşimi + `deploy/fetch_ddragon.py` api_contract §8'de. Rating'e sıfır etki. NOT: canlı yakalama için exe yeniden derleme/dağıtım gerekir (GÖREV 13 dağıtımıyla birleştirilebilir). **Ek [collector-worker → orkestratör, 2026-08-14, teknik düzeltme ONAY]:** `0` değerli boş slotlar YALNIZ MH'de değil her iki kaynakta da atılır (gerçek EOG boş slotu 0 taşıyor; aksi top_items'ı bozar ve EOG/MH tutarsızlığı yaratırdı). ingest_contract netleştirildi.

- [Teoman → orkestratör] [2026-08-14] **GÖREV 13 (new_modules.md): collector sağlık paneli — TAM sürüm.** Teoman kararları (AskUserQuestion): (1) cihaz kimliği SİHİRBAZDA sorulur (`.env` `CLIENT_ID`; yoksa hostname fallback); (2) ingest'e OPSİYONEL `client_id` alanı eklenir (hangi maç hangi PC'den — geriye uyumlu, eski exe'ler göndermez); (3) panel web UI'da AYRI DETAY GÖRÜNÜMÜ olur. **Orkestratör tanımları (teknik):** `POST /health/heartbeat` (client_id zorunlu; last_seen SUNUCUDA atanır; upsert) + `GET /health/collectors` (son görülme azalan; cihazın son maç izi `matches.client_id`'den); migration 0004 = `collector_health` tablosu + `matches.client_id` (nullable). Heartbeat anları: LCU bağlantısı, canlı modda her HEARTBEAT_MINUTES (varsayılan 5, 0=kapalı), backfill bitimi; heartbeat hatası collector'ı durdurmaz. Ham `ingest_events` değişmez. Spec: api_contract §6, ingest_contract "client_id", db_schema migration 0004. NOT: özellik canlıda ancak exe yeniden derlenip dağıtılınca tam etkinleşir.

- [Teoman → orkestratör] [2026-08-14] **GÖREV 11+12 (new_modules.md): rozetler + maçın MVP'si (birlikte).** Teoman kararları: orkestratörün önerdiği 10'luk rozet seti AYNEN onaylandı + **"Sonsuz Bench"** eklendi (3 maç üst üste kendi takımının en kötü oyuncusu); "maçın rekoru" rozetleri (vizyon/hasar/CS-dk/gold) 10 OYUNCU arasından seçilir (MVP ise kazanan takımdan — önceki karar); rozetler profilde vitrin, ayrı sayfa yok. **Orkestratör tanımları (teknik):** rozetler DB'ye YAZILMAZ — salt-okur türetilmiş hesap (`GET /players/{id}/badges`), replay determinizmi şart; "en kötü" ölçütü perf_score (tek başına en düşük; eşitlik bench SAYILMAZ ve seriyi kırar; takımda NULL perf seriyi kırar); MVP kırılımı perf → K → A → D az → player_id; pozitif rekor rozetlerinde eşitlikte herkes alır; blok rozetleri (5'li galibiyet, 3'lü bench) ayrık bloklarla sayılır. Katalog + tüm kurallar: api_contract §2 "Rozetler". DB/rating/collector değişikliği YOK. **Ek [backend-worker → orkestratör, 2026-08-14, teknik düzeltme ONAY]:** MVP kırılımında NULL k/d/a en kötü sayılır; cs_per_min'de duration NULL da dışlanır; takım-boyutu==5 şartı savunmacı kodlandı (ingest zaten dayatıyor). Contract'a işlendi.

- [Teoman → orkestratör] [2026-08-14] **GÖREV 10 (new_modules.md): profilde rating tarihçesi grafiği.** Teoman kararları: grafik EFEKTİF SCORE çizer (leaderboard değeriyle aynı tanım; AskUserQuestion onayı); dinamik zaman aralığı seçimi İSTEMCİ tarafında; veri noktasına tık → maç popup'ı (KDA vb.), popup'a ikinci tık → maç detayına gider. **Orkestratör tanımları (teknik):** yeni salt-okur endpoint `GET /players/{id}/rating-history` — valid maçlar, replay sort-key sırasında, nokta başına `score_after` (kümülatif P_avg ile tarihsel mu_eff; hesap rating paketi yardımcılarıyla, formül backend'e kopyalanmaz) + popup verisi (win/champion/position/k-d-a). Profilden maç detayına atlama için `GET /matches/{id}` (liste elemanıyla aynı şekil) eklendi. Grafik framework'süz satır içi SVG (KS1 diliyle). DB/rating/collector değişikliği YOK. Spec: api_contract §2 "Rating tarihçesi" + §3. Sıradaki görevler için de kararlar alındı: GÖREV 12 MVP = kazanan takımın en yüksek perf_score'lusu; GÖREV 11 rozet setini orkestratör önerecek; GÖREV 13 tam sürüm (collector heartbeat, ayrı iş).

- [Teoman → orkestratör] [2026-08-13] **GÖREV 7 REVİZYONU: taze repo İPTAL — mevcut repo ileride public yapılacak.** Teoman taze-repo fikrinden vazgeçti; kilit unsur: **14 arkadaşın rızası ALINDI** (Riot ID/puuid'lerin tarihçede görünmesi kabul) → tarihçe rewrite GEREKMEZ. Kalan flip-öncesi işler: (1) VPS sertleştirme — adımlar `deploy/VPS_HARDENING.md`'de (IP/secret içermez, public kalabilir); 6443 kısıtlaması CI deploy'un Actions'tan kubectl bağlantısına bağımlı → önerilen kalıcı çözüm SSH-tabanlı deploy'a geçiş (Teoman isterse orkestratör hazırlar). (2) Repo: `deploy/VPS_AGENT_BRIEF.md` + `VPS_DEPLOY_REPORT.md` kaldırılacak, `seed_roster.json` → example, `*_real.json` fixture'ları anonimleştirilecek, PR/issue metinleri gözden geçirilecek. (3) Ayarlar: branch protection, Actions fork onayı, GHCR paketi private teyidi. Doğrulanan güvenceler: tarihçede PAT/secret/`.env`/DB YOK; `API_KEY` yalnız k8s secret + kullanıcı localStorage + gitignore'lu `.env`'lerde — repo flip'i anahtarı SIZDIRMAZ; PAT revoke zorunlu değil (hijyen), aktif ghcr-pull PAT'ine dokunulmaz.

- [Teoman → orkestratör] [2026-08-13] **Kaçan maçlar: açılışta oto-yetişme + sıra-dışı ingest auto-replay.** Sorun (Teoman): collector yalnız açıkken maç yakalıyor; kapalıyken oynananlar geriye dönük gelmiyor. Tespit (orkestratör): `--backfill` modu zaten var ve match history sunucu tarafında durduğu için kapalıyken oynananları görebilir; ama çift tıklama yalnız canlı modu açtığından kimse koşmuyor. Kararlar (Teoman, 2026-08-13): **(1) Oto-yetişme** — canlı mod, LCU'ya her bağlandığında canlı döngüye geçmeden önce sınırlı backfill koşar (varsayılan son 14 gün, `CATCHUP_DAYS` ile ayarlanabilir, 0=kapalı); yetişme hatası canlı modu ENGELLEMEZ. `--backfill` tam/elle tarama için kalır; tiresiz `backfill` pozitif komutu alias olarak eklenir (dokümantasyonda tiresiz biçim yanlışlıkla anıldığı için). **(2) Sıra-dışı ingest auto-replay** — 2026-08-11'de ertelenen karar devreye alındı (çok-PC dönemi başladı): backend, duplicate olmayan maç mevcut en yeni valid maçtan ESKİ `played_at` ile gelirse incremental yerine İKİ evreni otomatik replay eder; yanıt şekli değişmez, determinizm korunur. Spec: ingest_contract "Kurallar", api_contract §5. Belirti: harici collector exe'sinden gelen maçta yalnız JUNGLE'lar rol aldı. Kök neden (Teoman teşhisi, orkestratör kanıt dosyasında birebir doğruladı — gameId 1734940206): o client'ta EOG `selectedPosition` 10/10 BOŞ string; `detectedTeamPosition` ise 10/10 dolu ve tutarlı (takım başına tam rol seti, Smite=JUNGLE). `_explicit_position` bu alanı bilmediğinden zincire düşülüyor, EOG formatında zincir yalnız Smite adımını çözebiliyor. **Karar: rol önceliğine orta katman eklendi** — açık seçim > `detectedTeamPosition` (Riot tespiti) > kısıt zinciri; backfill-positions aynı önceliği alır. ingest_contract.md güncellendi. Fixture: kanıt EOG'u ANONİMLEŞTİRİLMİŞ olarak eklenir (yeni patch şekli; GÖREV 7 sonrası kural: gerçek PII'lı fixture commit'lenmez). Not: düzeltme sonrası harici kullanıcıya yeni exe + maç 1734940206 için backfill-positions/manuel düzeltme gerekir; GÖREV 7 public snapshot'ı bu fix'ten sonra tazelenmelidir.

- [Teoman → orkestratör] [2026-08-12] **GÖREV 7 (new_modules.md): repo public yapılacak — TAZE REPO stratejisi.** Denetim (3 agent + orkestratör doğrulaması) tamam: tarihçede secret YOK (token/env/db hiçbir rev'de yok — bağımsız doğrulandı); ENGEL = 14 arkadaş puuid+RiotID'si 6 dosyada ~40 commit'te + VPS_DEPLOY_REPORT.md'de gerçek IP ve "6443 açık" itirafı; PR/issue metinleri de flip'le görünür olur. **Karar (Teoman): taze public repo** — mevcut repo private arşiv kalır, temiz tek-commit'lik yeni repo yayınlanır. Orkestratör varsayılanları (veto edilebilir): _real fixture'lar deterministik anonimleştirilir (şema/gameId/timestamp korunur, testler yeşil), seed_roster → example şablon, api_contract örnek adları sahte, mock isimleri kalır, VPS_DEPLOY_REPORT.md + VPS_AGENT_BRIEF.md public'e ALINMAZ (k8s.yaml + ci-rbac.yaml girer), GHCR paketi private kalır (VPS ghcr-pull PAT'i aynen). Teoman'a kalan flip adımları: yeni repoya KUBE_CONFIG_B64 secret'ı, GHCR paketine yeni repo Actions erişimi, ErtugrulSelim collaborator, ad değişimi (arşiv→lol-balance-archive, yeni→lol-balance), visibility flip, main branch protection. VPS'te repo'dan bağımsız sertleştirme önerisi: :6443'ü allowlist'le + eski ghp_/workflow PAT'lerini revoke et.

- [Teoman → orkestratör] [2026-08-12] **GÖREV 6 (new_modules.md): tam İngilizce çeviri + EN/TR dil altyapısı.** Teoman kararları: **(1) Varsayılan dil TR** (seçim localStorage'da kalıcı). **(2) Kapsam: webui + kök README + collector sihirbazı** — sihirbazın ilk sorusu İngilizce dil seçimi (`tr`/`en` terminal girişi), devamı seçilen dilde; config'e yazılır, tekrar sorulmaz. **(3) README: EN asıl (`README.md`) + TR ayna (`README.tr.md`), çapraz linkli.** Orkestratör kararları (teknik): framework'süz sözlük tabanlı i18n (`webui/i18n/`, `I18n.t()`, `data-i18n`); backend yanıtları lokalize edilmez; sözlük bütünlüğü CI'da pytest ile zorlanır; gelecek modüller için bağlayıcı kural kondu. Tam spec: `docs/i18n_contract.md` (YENİ). Backend/rating/DB/ingest değişmez; collector exe'si iş bitince yeniden paketlenir.

- [Teoman → orkestratör] [2026-08-12] **GÖREV 5 (new_modules.md): collector'ın taşınabilir paketlenmesi.** Teoman kararları: **(1) Tek .exe** (PyInstaller onefile; Python/derleme gerektirmez, SmartScreen uyarısı kabul edildi). **(2) İlk açılış sihirbazı** — .env yoksa terminalde sorar (API anahtarı, backend adresi varsayılan canlı URL, LoL klasörü kayıt defteri/bilinen yollardan otomatik aranır), .env'i exe'nin yanına yazar. **(3) Elle çalıştırma** — zamanlanmış görev KURULMAZ (oyun akşamı açılır; kaçan maç idempotent backfill ile telafi). Kapsam yalnız collector/; backend/webui/rating ve ingest contract'ı değişmez. Kritik teknik not: onefile modda paket-içi yollar (raw_archive, outbox, .env) sys._MEIPASS geçici dizinine düşer — frozen'da exe-bitişiği dizin kullanılmalı.
- [Teoman → orkestratör] [2026-08-12] **GÖREV 4 (new_modules.md): harita üzerinde rol enleri sayfası** — LoL haritası illüstrasyonu, koridor baloncuklarında o rolün en iyisi, baloncuğa tıklayınca rol sıralaması pop-up'ı. **Orkestratör kararları (gösterim seviyesi, teknik):** (1) Harita Riot varlığı DEĞİL — özgün, stilize, satır içi SVG (telif + kendi kendine yeterlilik; framework'süzlük korunur). (2) Veri kaynağı mevcut `GET /leaderboard` `role_ratings` alanı — YENİ ENDPOINT YOK, backend/rating/collector değişmez. (3) Rol sıralaması: o rolde ≥1 valid maçı olanlar, rol score azalan → maç sayısı azalan → ad; baloncuk = sıralamanın ilki, rolde kimse oynamadıysa "—". Pop-up'taki oyuncular profile gider. (4) Yerleşim: 6. sekme "Harita" (≥320px ölçümle sığarsa; sığmazsa Enler'den girişli detay görünümü — webui agent'ı ölçüp raporlar). GÖREV 1 ek satırı ("her rol başına rating profilde") mevcut profil rol şeridiyle ZATEN karşılanıyor, iş çıkmadı.

- [Teoman → orkestratör] [2026-08-12] **GÖREV 3 (new_modules.md): Nemesis.** Teoman'ın dört tasarım kararı: **(1) Ölçüt "önce başa-başlık"** — aynı koridorda karşı takımlarda ≥3 karşılaşma eşiği; closeness (galibiyet dengesi %50'ye yakınlık) azalan → karşılaşma sayısı azalan → deterministik kırılımlar. **(2) Pencere "ikisi birden"** — ekranda tüm zamanların çifti; maç önerisi haftalık çifti kullanır (haftalık pencere kuralı GÖREV 2 ile aynı), haftalık yoksa tüm zamanlara düşer (`active` alanı). **(3) Maç önerisi seçilen 10 kişiden** — çift karşı takımlara nemesis rolüne sabitlenir, kalan 8 normal rol-bazlı optimizasyonla dağıtılır (`POST /balance/nemesis`; rating paketine kısıtlı dengeleme varyantı). **(4) Gösterim Enler içinde bölüm** — çift kartı + "Nemesis maçı kur" düğmesi (Dengele'ye nemesis moduyla geçer); 6. sekme açılmaz. Nemesis GÖSTERİM + öneri katmanıdır; rating modeline dokunmaz (nemesis maçı normal maç olarak rating'e girer, özel ağırlık YOK). Spec: api_contract "Nemesis" + "Nemesis maçı", rating_contract "Dengeleme" §6. DB/collector değişikliği yok.

- [lcu-collector] [2026-08-12] Gerçek canlı EOG fixture'ı (1734664864) ile açık madde #1 kapandı; iki bulgu: (a) `normalize_eog` `played_at`'i çağıranın `captured_at`'inden üretiyor — outbox/retry gecikmesinde maç zamanı kayar, oysa gerçek EOG `endOfGameTimestamp` taşıyor; (b) `backfill-positions` EOG arşivlerinde açık `selectedPosition` dururken yalnız tahmin zincirini koşuyor. **Karar: [orkestratör, 2026-08-12] İKİSİ DE ONAY (teknik düzeltme).** (a) `played_at` önceliği: `endOfGameTimestamp` varsa o, yoksa mevcut `captured_at` davranışı — replay sırası ve haftalık pencere doğruluğu için; contract anlamı değişmiyor (ingest_contract "maç bitiş zamanı" zaten diyor). (b) Backfill EOG kayıtlarında açık pozisyonu okur ("açık alan kazanır" kuralının backfill yoluna uygulanması). Collector'a uygulatıldı.
- [Teoman → orkestratör] [2026-08-12] **GÖREV 2 (new_modules.md): haftanın enleri ekranı** — haftanın en iyi oyuncusu, "yıldız rukisi" (en fazla rating yükselişi), rol bazında haftanın enleri; tıklayınca profile gider. **Orkestratör tanımları (gösterim seviyesi, teknik):** pencere = son 7 gün (UTC, rolling); pencere boşsa son valid maça çapalanır (`fallback: true`) — ekran veri varken boş kalmaz. En iyi oyuncu = pencerede oynayanlar arasında güncel score; yıldız rukisi = pencere içi ordinal (mu−3σ) artışı (ana evren, blend P_avg terimi karışıklık yaratmasın diye çekirdek delta); rol enleri = o rolde pencerede oynayanlar arasında güncel rol score'u. Deterministik eşitlik kırılımları contract'ta. Yeni endpoint `GET /highlights/weekly` (api_contract §2). DB/rating/collector değişikliği yok.

- [Teoman → orkestratör] [2026-08-12] **GÖREV 1 (new_modules.md): oyuncu profil ekranı** — sıralamada oyuncuya tıklayınca profil; ortalama KDA, W/L, favori karakter, favori koridor, en sinerjili takım arkadaşı. **Orkestratör metrik tanımları (gösterim seviyesi, teknik):** KDA ratio = (ΣK+ΣA)/max(1,ΣD) statlı valid maçlardan; favoriler mod (null'lar hariç, deterministik eşitlik kırılımı); sinerji = aynı takımda ≥2 ortak valid maçta galibiyet oranı (winrate → ortak maç → ad sıralı, ilk 3) — yalnız GÖSTERİMDİR, rating'e girmez; Faz 2 pair-synergy rating modeli kapsam dışı kalmaya devam eder. Yeni endpoint `GET /players/{id}/stats` (api_contract §2). DB/rating/collector değişikliği yok.

Agent'lar contract değişiklik önerilerini buraya ekler. Format:
- [agent-adı] [tarih] Sorun + önerilen değişiklik. Karar: (orkestratör doldurur)

- [lcu-collector] [2026-08-11] Backfill roster filtresi brief'e göre "backend `GET /players`'tan puuid'ler" ile kurulmalı, ancak `api_contract.md`'de `GET /players` yanıtında `puuid` alanı yok (yalnızca `riot_id`). Bloklayıcı değil: collector şimdilik `riot_id` ile eşleştiriyor ve yanıtta `puuid` alanı görürse ileriye dönük olarak onu da kullanıyor. Öneri: `GET /players` yanıtına opsiyonel `puuid` alanı eklensin (riot_id değişebilir/yeniden adlandırılabilir, puuid kalıcıdır). **Karar: [orkestratör, 2026-08-11] ONAY (teknik düzeltme).** Var olan DB kolonunu açığa çıkarır, davranış değişikliği değildir; puuid kalıcı kimlik olduğu için doğru eşleştirme anahtarıdır. `api_contract.md` §2 güncellendi; backend `PlayerOut`'a nullable `puuid` ekleyecek. Collector'ın mevcut "puuid görürse kullan" davranışı zaten uyumlu, collector'da değişiklik gerekmez.

- [web-ui] [2026-08-11] `api_contract.md` §3'te `GET /matches?limit=20` yanıtı yalnızca sözle tarif edilmiş ("katılımcılar ve rating değişimleriyle"), alan bazlı örnek yok. Bloklayıcı değil: web UI (ve mock'u) şu şekli varsayıyor; backend farklı dönerse UI'daki alan adları güncellenmeli. Öneri: bu örnek contract'a eklensin.
  ```json
  [{
    "id": 42, "source_game_id": "6874231955", "played_at": "2026-08-11T20:41:03Z",
    "duration_s": 1874, "winner_team": 100, "status": "valid",
    "participants": [{
      "player_id": 1, "display_name": "Teoman", "team": 100,
      "position": "MIDDLE", "champion": "Ahri",
      "mu_before": 25.0, "mu_after": 26.1
    }]
  }]
  ```
  **Karar: [orkestratör, 2026-08-11] ÖRNEK EKLENDİ, ANCAK KANONİK ŞEKİL BACKEND'İN MEVCUT ŞEKLİDİR (teknik düzeltme).** Web UI'ın varsaydığı düz `mu_before/mu_after` yerine backend'in döndürdüğü iç içe `stats` + nullable `rating_change` nesnesi contract'a yazıldı (`api_contract.md` §3). Gerekçe: `rating_change: null`, void/ratingsiz maçı ifade edebilir — düz alanlar edemez; `stats` gruplaması ingest_contract ile tutarlıdır. Aksiyon: web UI `app.js` ve `mock_api.js` bu şekle uyarlanacak (backend değişmez).

- [orkestratör] [2026-08-11] Canlı backfill bulgusu: maçlar kronolojik sıra dışında ingest edilirse (ör. ikinci PC'den eski maç gelirse) incremental rating bayat kalıyor; düzeltmek manuel `POST /admin/replay` gerektiriyor. Collector tarafı eskiden-yeniye gönderimle düzeltildi (contract değişikliği değil). Backend'in sıra-dışı geliş tespitinde OTOMATİK replay koşması önerisi ise davranışsal değişikliktir. **Karar: [Teoman, 2026-08-11] ERTELENDİ** — "şu an gerek yok, not al, daha sonra bakacağız." Çoklu-PC kurulumu gündeme geldiğinde yeniden değerlendirilecek.
- [Teoman → orkestratör] [2026-08-11] Talep: KDA, hasar payı (maç toplamına oran) gibi metrikler sisteme girsin. Orkestratör seçenekleri sundu (yalnız gösterim / ayrı performans skoru / rating'e dahil). **Karar: [Teoman, 2026-08-11] RATING'E DE GİRSİN; metrikler: KDA, hasar payı, gold payı, CS/dk, vizyon.** Bu, "rating'e giren tek sinyal W/L" mimari kararının insan tarafından revizyonudur. Uygulama: yeni `openskill-pl-perf-v1` engine (W/L yönü korunur, performans güncelleme büyüklüğünü [0.7,1.3] bandında modüle eder) — tam spec: `docs/rating_contract.md`. CLAUDE.md karar #1 güncellendi. `openskill-pl-v1` değişmeden kalır.
- [Teoman → orkestratör] [2026-08-11] **GÖREV 0 (new_modules.md): koridor (rol) bazlı ikinci rating sistemi + rol bazlı dengeleme.** Veri bulgusu: mevcut 10 gerçek maçın 100 katılımcı satırının tamamında `position` NULL; Riot'un `timeline.lane` tahmini kanıtlanabilir bozuk (36 JUNGLE etiketi, bunların 16'sı Smite'sız; TOP etiketi 100'de 7). Buna karşın Smite sinyali kusursuz (tam 20 taşıyıcı, hepsi gerçek ormancı). Kararlar (Teoman, 2026-08-11): **(1) Rol verisi HİBRİT** — collector kısıt-çözümlü tahmin yapar (Smite→JUNGLE, lane/role etiketleri, TOP=eleme ile kalan), web UI'dan manuel düzeltme, backend'e `PUT /matches/{id}/positions` (düzeltme → rol evreni replay). **(2) Rol rating formülü ana blend50 ile birebir aynı** — (player, role) başına ayrı mu/sigma/P_avg, ayrı tablo `role_rating_history` (migration 0003); ana rating'e dokunulmaz, iki evren ayrı hesaplanır. **(3) Dengeleme HER ZAMAN rol bazlı** — eski salt-rating modu kaldırıldı; her öneri takım + rol ataması döner. Edge-case minimalizmi Teoman talebidir ("şu an bu yeni sistemle dümdüz çalışması yeter"). Spec: `rating_contract.md` "Rol Rating Evreni", `api_contract.md` §2–§5, `db_schema.md` migration 0003, `ingest_contract.md` tahmin notu.

- [lcu-collector] [2026-08-11] Rol tahmin zinciri 10 gerçek maçta 17/20 takım çözüyor (94/100 pozisyon); Smite→JUNGLE 20/20 kusursuz. Öneri: TOP elemesinden önce "kalanlar içinde tam 1 `lane=TOP` etiketli → TOP, sonra son kalan → kalan tek rol" adımı → 19/20 (98/100). Kalan tek vaka (iki oyuncu da `TOP/DUO` etiketli) hiçbir kuralla ayırt edilemez, manuel düzeltmeye kalır. **Karar: [orkestratör, 2026-08-11] ONAY (teknik düzeltme).** API/şema/rating anlamı değişmiyor; onaylı hibrit tasarımın içinde aynı sinyal sınıfıyla deterministik zincir iyileştirmesi. Belirsizlikte null ve manuel düzeltmenin üstünlüğü korunuyor. `ingest_contract.md` zincir tanımı güncellendi; collector'a uygulatıldı.

- [Teoman → orkestratör] [2026-08-11] İkinci rating kararı: perf-v1'in çarpan yaklaşımı yetersiz bulundu ("iyi oyuncuyla aynı takımda oynayan bir anda tırmanıyor"); W/L %50 + performans %50 doğrudan katkı istendi. Orkestratör üç formül sundu; **Karar: [Teoman, 2026-08-11] HARMAN PUANI** — `openskill-pl-blend50-v1`: saf W/L mu/sigma çekirdeği + kariyer perf ortalamasından efektif rating (`mu_eff = 0.5*mu + 0.5*(25+20*(P_avg-1))`, `score = mu_eff - 3*sigma`). Stat kasma teşviki ve "iyi kaybeden > kötü kazanan" ihtimali ödünleşim olarak kabul edildi. Spec: rating_contract.md "Harman Engine"; api_contract rating nesnesi + leaderboard sıralaması, db_schema perf_score kolonu (migration 0002) güncellendi. CLAUDE.md karar #1 ikinci kez revize edildi.
