# Change Requests

- [Teoman → orkestratör] [2026-08-13] **Kaçan maçlar: açılışta oto-yetişme + sıra-dışı ingest auto-replay.** Sorun (Teoman): collector yalnız açıkken maç yakalıyor; kapalıyken oynananlar geriye dönük gelmiyor. Tespit (orkestratör): `--backfill` modu zaten var ve match history sunucu tarafında durduğu için kapalıyken oynananları görebilir; ama çift tıklama yalnız canlı modu açtığından kimse koşmuyor. Kararlar (Teoman, 2026-08-13): **(1) Oto-yetişme** — canlı mod, LCU'ya her bağlandığında canlı döngüye geçmeden önce sınırlı backfill koşar (varsayılan son 14 gün, `CATCHUP_DAYS` ile ayarlanabilir, 0=kapalı); yetişme hatası canlı modu ENGELLEMEZ. `--backfill` tam/elle tarama için kalır; tiresiz `backfill` pozitif komutu alias olarak eklenir (dokümantasyonda tiresiz biçim yanlışlıkla anıldığı için). **(2) Sıra-dışı ingest auto-replay** — 2026-08-11'de ertelenen karar devreye alındı (çok-PC dönemi başladı): backend, duplicate olmayan maç mevcut en yeni valid maçtan ESKİ `played_at` ile gelirse incremental yerine İKİ evreni otomatik replay eder; yanıt şekli değişmez, determinizm korunur. Spec: ingest_contract "Kurallar", api_contract §5. Belirti: harici collector exe'sinden gelen maçta yalnız JUNGLE'lar rol aldı. Kök neden (Teoman teşhisi, orkestratör kanıt dosyasında birebir doğruladı — gameId 1734940206): o client'ta EOG `selectedPosition` 10/10 BOŞ string; `detectedTeamPosition` ise 10/10 dolu ve tutarlı (takım başına tam rol seti, Smite=JUNGLE). `_explicit_position` bu alanı bilmediğinden zincire düşülüyor, EOG formatında zincir yalnız Smite adımını çözebiliyor. **Karar: rol önceliğine orta katman eklendi** — açık seçim > `detectedTeamPosition` (Riot tespiti) > kısıt zinciri; backfill-positions aynı önceliği alır. ingest_contract.md güncellendi. Fixture: kanıt EOG'u ANONİMLEŞTİRİLMİŞ olarak eklenir (yeni patch şekli; GÖREV 7 sonrası kural: gerçek PII'lı fixture commit'lenmez). Not: düzeltme sonrası harici kullanıcıya yeni exe + maç 1734940206 için backfill-positions/manuel düzeltme gerekir; GÖREV 7 public snapshot'ı bu fix'ten sonra tazelenmelidir.

- [Teoman → orkestratör] [2026-08-12] **GÖREV 7 (new_modules.md): repo public yapılacak — TAZE REPO stratejisi.** Denetim (3 agent + orkestratör doğrulaması) tamam: tarihçede secret YOK (token/env/db hiçbir rev'de yok — bağımsız doğrulandı); ENGEL = 14 arkadaş puuid+RiotID'si 6 dosyada ~40 commit'te + VPS_DEPLOY_REPORT.md'de gerçek IP ve "6443 açık" itirafı; PR/issue metinleri de flip'le görünür olur. **Karar (Teoman): taze public repo** — mevcut repo private arşiv kalır, temiz tek-commit'lik yeni repo yayınlanır. Orkestratör varsayılanları (veto edilebilir): _real fixture'lar deterministik anonimleştirilir (şema/gameId/timestamp korunur, testler yeşil), seed_roster → example şablon, api_contract örnek adları sahte, mock isimleri kalır, VPS_DEPLOY_REPORT.md + VPS_AGENT_BRIEF.md public'e ALINMAZ (k8s.yaml + ci-rbac.yaml girer), GHCR paketi private kalır (VPS ghcr-pull PAT'i aynen). Teoman'a kalan flip adımları: yeni repoya KUBE_CONFIG_B64 secret'ı, GHCR paketine yeni repo Actions erişimi, ErtugrulSelim collaborator, ad değişimi (arşiv→lol-balance-archive, yeni→lol-balance), visibility flip, main branch protection. VPS'te repo'dan bağımsız sertleştirme önerisi: :6443'ü allowlist'le + eski ghp_/workflow PAT'lerini revoke et.

- [Teoman → orkestratör] [2026-08-12] **GÖREV 6 (new_modules.md): tam İngilizce çeviri + EN/TR dil altyapısı.** Teoman kararları: **(1) Varsayılan dil TR** (seçim localStorage'da kalıcı). **(2) Kapsam: webui + kök README + collector sihirbazı** — sihirbazın ilk sorusu İngilizce dil seçimi (`tr`/`en` terminal girişi), devamı seçilen dilde; config'e yazılır, tekrar sorulmaz. **(3) README: EN asıl (`README.md`) + TR ayna (`README.tr.md`), çapraz linkli.** Orkestratör kararları (teknik): framework'süz sözlük tabanlı i18n (`webui/i18n/`, `I18n.t()`, `data-i18n`); backend yanıtları lokalize edilmez; sözlük bütünlüğü CI'da pytest ile zorlanır; gelecek modüller için bağlayıcı kural kondu. Tam spec: `docs/i18n_contract.md` (YENİ). Backend/rating/DB/ingest değişmez; collector exe'si iş bitince yeniden paketlenir.

- [Teoman → orkestratör] [2026-08-12] **GÖREV 5 (new_modules.md): collector'ın taşınabilir paketlenmesi.** Teoman kararları: **(1) Tek .exe** (PyInstaller onefile; Python/derleme gerektirmez, SmartScreen uyarısı kabul edildi). **(2) İlk açılış sihirbazı** — .env yoksa terminalde sorar (API anahtarı, backend adresi varsayılan canlı URL, LoL klasörü kayıt defteri/bilinen yollardan otomatik aranır), .env'i exe'nin yanına yazar. **(3) Elle çalıştırma** — zamanlanmış görev KURULMAZ (oyun akşamı açılır; kaçan maç idempotent backfill ile telafi). Kapsam yalnız collector/; backend/webui/rating ve ingest contract'ı değişmez. Kritik teknik not: onefile modda paket-içi yollar (raw_archive, outbox, .env) sys._MEIPASS geçici dizinine düşer — frozen'da exe-bitişiği dizin kullanılmalı.
- [Teoman → orkestratör] [2026-08-12] **GÖREV 4 (new_modules.md): harita üzerinde rol enleri sayfası** — LoL haritası illüstrasyonu, koridor baloncuklarında o rolün en iyisi, baloncuğa tıklayınca rol sıralaması pop-up'ı. **Orkestratör kararları (gösterim seviyesi, teknik):** (1) Harita Riot varlığı DEĞİL — özgün, stilize, satır içi SVG (telif + kendi kendine yeterlilik; framework'süzlük korunur). (2) Veri kaynağı mevcut `GET /leaderboard` `role_ratings` alanı — YENİ ENDPOINT YOK, backend/rating/collector değişmez. (3) Rol sıralaması: o rolde ≥1 valid maçı olanlar, rol score azalan → maç sayısı azalan → ad; baloncuk = sıralamanın ilki, rolde kimse oynamadıysa "—". Pop-up'taki oyuncular profile gider. (4) Yerleşim: 6. sekme "Harita" (≥320px ölçümle sığarsa; sığmazsa Enler'den girişli detay görünümü — webui agent'ı ölçüp raporlar). GÖREV 1 ek satırı ("her rol başına rating profilde") mevcut profil rol şeridiyle ZATEN karşılanıyor, iş çıkmadı.

- [Teoman → orkestratör] [2026-08-12] **GÖREV 3 (new_modules.md): Nemesis.** Teoman'ın dört tasarım kararı: **(1) Ölçüt "önce başa-başlık"** — aynı koridorda karşı takımlarda ≥3 karşılaşma eşiği; closeness (galibiyet dengesi %50'ye yakınlık) azalan → karşılaşma sayısı azalan → deterministik kırılımlar. **(2) Pencere "ikisi birden"** — ekranda tüm zamanların çifti; maç önerisi haftalık çifti kullanır (haftalık pencere kuralı GÖREV 2 ile aynı), haftalık yoksa tüm zamanlara düşer (`active` alanı). **(3) Maç önerisi seçilen 10 kişiden** — çift karşı takımlara nemesis rolüne sabitlenir, kalan 8 normal rol-bazlı optimizasyonla dağıtılır (`POST /balance/nemesis`; rating paketine kısıtlı dengeleme varyantı). **(4) Gösterim Enler içinde bölüm** — çift kartı + "Nemesis maçı kur" düğmesi (Dengele'ye nemesis moduyla geçer); 6. sekme açılmaz. Nemesis GÖSTERİM + öneri katmanıdır; rating modeline dokunmaz (nemesis maçı normal maç olarak rating'e girer, özel ağırlık YOK). Spec: api_contract "Nemesis" + "Nemesis maçı", rating_contract "Dengeleme" §6. DB/collector değişikliği yok.

- [lcu-collector] [2026-08-12] Gerçek canlı EOG fixture'ı (1734664864) ile açık madde #1 kapandı; iki bulgu: (a) `normalize_eog` `played_at`'i çağıranın `captured_at`'inden üretiyor — outbox/retry gecikmesinde maç zamanı kayar, oysa gerçek EOG `endOfGameTimestamp` taşıyor; (b) `backfill-positions` EOG arşivlerinde açık `selectedPosition` dururken yalnız tahmin zincirini koşuyor. **Karar: [orkestratör, 2026-08-12] İKİSİ DE ONAY (teknik düzeltme).** (a) `played_at` önceliği: `endOfGameTimestamp` varsa o, yoksa mevcut `captured_at` davranışı — replay sırası ve haftalık pencere doğruluğu için; contract anlamı değişmiyor (ingest_contract "maç bitiş zamanı" zaten diyor). (b) Backfill EOG kayıtlarında açık pozisyonu okur ("açık alan kazanır" kuralının backfill yoluna uygulanması). Collector'a uygulatıldı.
- [Teoman → orkestratör] [2026-08-12] **GÖREV 2 (new_modules.md): haftanın enleri ekranı** — haftanın en iyi oyuncusu, "yıldız rukisi" (en fazla rating yükselişi), rol bazında haftanın enleri; tıklayınca profile gider. **Orkestratör tanımları (gösterim seviyesi, teknik):** pencere = son 7 gün (UTC, rolling); pencere boşsa son valid maça çapalanır (`fallback: true`) — ekran veri varken boş kalmaz. En iyi oyuncu = pencerede oynayanlar arasında güncel score; yıldız rukisi = pencere içi ordinal (mu−3σ) artışı (ana evren, blend P_avg terimi karışıklık yaratmasın diye çekirdek delta); rol enleri = o rolde pencerede oynayanlar arasında güncel rol score'u. Deterministik eşitlik kırılımları contract'ta. Yeni endpoint `GET /highlights/weekly` (api_contract §2). DB/rating/collector değişikliği yok.

- [Teoman → orkestratör] [2026-08-12] **GÖREV 1 (new_modules.md): oyuncu profil ekranı** — sıralamada oyuncuya tıklayınca profil; ortalama KDA, W/L, favori karakter, favori koridor, en sinerjili takım arkadaşı. **Orkestratör metrik tanımları (gösterim seviyesi, teknik):** KDA ratio = (ΣK+ΣA)/max(1,ΣD) statlı valid maçlardan; favoriler mod (null'lar hariç, deterministik eşitlik kırılımı); sinerji = aynı takımda ≥2 ortak valid maçta galibiyet oranı (winrate → ortak maç → ad sıralı, ilk 3) — yalnız GÖSTERİMDİR, rating'e girmez; Faz 2 pair-synergy rating modeli kapsam dışı kalmaya devam eder. Yeni endpoint `GET /players/{id}/stats` (api_contract §2). DB/rating/collector değişikliği yok.

Agent'lar contract değişiklik önerilerini buraya ekler. Format:
- [agent-adı] [tarih] Sorun + önerilen değişiklik. Karar: (orkestratör doldurur)

- [lcu-collector] [2026-08-11] Backfill roster filtresi brief'e göre "backend `GET /players`'tan puuid'ler" ile kurulmalı, ancak `api_contract.md`'de `GET /players` yanıtında `puuid` alanı yok (yalnızca `riot_id`). Bloklayıcı değil: collector şimdilik `riot_id` ile eşleştiriyor ve yanıtta `puuid` alanı görürse ileriye dönük olarak onu da kullanıyor. Öneri: `GET /players` yanıtına opsiyonel `puuid` alanı eklensin (riot_id değişebilir/yeniden adlandırılabilir, puuid kalıcıdır). **Karar: [orkestratör, 2026-08-11] ONAY (teknik düzeltme).** Var olan DB kolonunu açığa çıkarır, davranış değişikliği değildir; puuid kalıcı kimlik olduğu için doğru eşleştirme anahtarıdır. `api_contract.md` §2 güncellendi; backend `PlayerOut`'a nullable `puuid` ekleyecek. Collector'ın mevcut "puuid görürse kullan" davranışı zaten uyumlu, collector'da değişiklik gerekmez.

- [web-ui] [2026-08-11] `api_contract.md` §3'te `GET /matches?limit=20` yanıtı yalnızca sözle tarif edilmiş ("katılımcılar ve rating değişimleriyle"), alan bazlı örnek yok. Bloklayıcı değil: web UI (ve mock'u) şu şekli varsayıyor; backend farklı dönerse UI'daki alan adları güncellenmeli. Öneri: bu örnek contract'a eklensin.
  ```json
  [{
    "id": 42, "source_game_id": "6874231955", "played_at": "2026-08-11T20:41:03Z",
    "duration_s": 1874, "winner_team": 100, "status": "valid",
    "participants": [{
      "player_id": 1, "display_name": "Teoman", "team": 100,
      "position": "MIDDLE", "champion": "Ahri",
      "mu_before": 25.0, "mu_after": 26.1
    }]
  }]
  ```
  **Karar: [orkestratör, 2026-08-11] ÖRNEK EKLENDİ, ANCAK KANONİK ŞEKİL BACKEND'İN MEVCUT ŞEKLİDİR (teknik düzeltme).** Web UI'ın varsaydığı düz `mu_before/mu_after` yerine backend'in döndürdüğü iç içe `stats` + nullable `rating_change` nesnesi contract'a yazıldı (`api_contract.md` §3). Gerekçe: `rating_change: null`, void/ratingsiz maçı ifade edebilir — düz alanlar edemez; `stats` gruplaması ingest_contract ile tutarlıdır. Aksiyon: web UI `app.js` ve `mock_api.js` bu şekle uyarlanacak (backend değişmez).

- [orkestratör] [2026-08-11] Canlı backfill bulgusu: maçlar kronolojik sıra dışında ingest edilirse (ör. ikinci PC'den eski maç gelirse) incremental rating bayat kalıyor; düzeltmek manuel `POST /admin/replay` gerektiriyor. Collector tarafı eskiden-yeniye gönderimle düzeltildi (contract değişikliği değil). Backend'in sıra-dışı geliş tespitinde OTOMATİK replay koşması önerisi ise davranışsal değişikliktir. **Karar: [Teoman, 2026-08-11] ERTELENDİ** — "şu an gerek yok, not al, daha sonra bakacağız." Çoklu-PC kurulumu gündeme geldiğinde yeniden değerlendirilecek.
- [Teoman → orkestratör] [2026-08-11] Talep: KDA, hasar payı (maç toplamına oran) gibi metrikler sisteme girsin. Orkestratör seçenekleri sundu (yalnız gösterim / ayrı performans skoru / rating'e dahil). **Karar: [Teoman, 2026-08-11] RATING'E DE GİRSİN; metrikler: KDA, hasar payı, gold payı, CS/dk, vizyon.** Bu, "rating'e giren tek sinyal W/L" mimari kararının insan tarafından revizyonudur. Uygulama: yeni `openskill-pl-perf-v1` engine (W/L yönü korunur, performans güncelleme büyüklüğünü [0.7,1.3] bandında modüle eder) — tam spec: `docs/rating_contract.md`. CLAUDE.md karar #1 güncellendi. `openskill-pl-v1` değişmeden kalır.
- [Teoman → orkestratör] [2026-08-11] **GÖREV 0 (new_modules.md): koridor (rol) bazlı ikinci rating sistemi + rol bazlı dengeleme.** Veri bulgusu: mevcut 10 gerçek maçın 100 katılımcı satırının tamamında `position` NULL; Riot'un `timeline.lane` tahmini kanıtlanabilir bozuk (36 JUNGLE etiketi, bunların 16'sı Smite'sız; TOP etiketi 100'de 7). Buna karşın Smite sinyali kusursuz (tam 20 taşıyıcı, hepsi gerçek ormancı). Kararlar (Teoman, 2026-08-11): **(1) Rol verisi HİBRİT** — collector kısıt-çözümlü tahmin yapar (Smite→JUNGLE, lane/role etiketleri, TOP=eleme ile kalan), web UI'dan manuel düzeltme, backend'e `PUT /matches/{id}/positions` (düzeltme → rol evreni replay). **(2) Rol rating formülü ana blend50 ile birebir aynı** — (player, role) başına ayrı mu/sigma/P_avg, ayrı tablo `role_rating_history` (migration 0003); ana rating'e dokunulmaz, iki evren ayrı hesaplanır. **(3) Dengeleme HER ZAMAN rol bazlı** — eski salt-rating modu kaldırıldı; her öneri takım + rol ataması döner. Edge-case minimalizmi Teoman talebidir ("şu an bu yeni sistemle dümdüz çalışması yeter"). Spec: `rating_contract.md` "Rol Rating Evreni", `api_contract.md` §2–§5, `db_schema.md` migration 0003, `ingest_contract.md` tahmin notu.

- [lcu-collector] [2026-08-11] Rol tahmin zinciri 10 gerçek maçta 17/20 takım çözüyor (94/100 pozisyon); Smite→JUNGLE 20/20 kusursuz. Öneri: TOP elemesinden önce "kalanlar içinde tam 1 `lane=TOP` etiketli → TOP, sonra son kalan → kalan tek rol" adımı → 19/20 (98/100). Kalan tek vaka (iki oyuncu da `TOP/DUO` etiketli) hiçbir kuralla ayırt edilemez, manuel düzeltmeye kalır. **Karar: [orkestratör, 2026-08-11] ONAY (teknik düzeltme).** API/şema/rating anlamı değişmiyor; onaylı hibrit tasarımın içinde aynı sinyal sınıfıyla deterministik zincir iyileştirmesi. Belirsizlikte null ve manuel düzeltmenin üstünlüğü korunuyor. `ingest_contract.md` zincir tanımı güncellendi; collector'a uygulatıldı.

- [Teoman → orkestratör] [2026-08-11] İkinci rating kararı: perf-v1'in çarpan yaklaşımı yetersiz bulundu ("iyi oyuncuyla aynı takımda oynayan bir anda tırmanıyor"); W/L %50 + performans %50 doğrudan katkı istendi. Orkestratör üç formül sundu; **Karar: [Teoman, 2026-08-11] HARMAN PUANI** — `openskill-pl-blend50-v1`: saf W/L mu/sigma çekirdeği + kariyer perf ortalamasından efektif rating (`mu_eff = 0.5*mu + 0.5*(25+20*(P_avg-1))`, `score = mu_eff - 3*sigma`). Stat kasma teşviki ve "iyi kaybeden > kötü kazanan" ihtimali ödünleşim olarak kabul edildi. Spec: rating_contract.md "Harman Engine"; api_contract rating nesnesi + leaderboard sıralaması, db_schema perf_score kolonu (migration 0002) güncellendi. CLAUDE.md karar #1 ikinci kez revize edildi.
